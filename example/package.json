{
  "name": "type-ui-example",
  "version": "1.0.0",
  "description": "Todos app using type-ui",
  "scripts": {
    "server": "json-server -w db.json",
    "start": "parcel index.html"
  },
  "dependencies": {
    "type-ui": "file:.."
  },
  "devDependencies": {
    "json-server": "^0.15.0",
    "parcel-bundler": "^1.12.3",
    "typescript": "^3.5.2"
  }
}
